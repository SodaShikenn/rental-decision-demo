// Copy this file to config.js and add a browser-restricted Google Maps API key.
// Do not commit config.js.
window.RENTAL_DEMO_CONFIG = {
  googleMapsApiKey: "",
  // Base URL of the listing-extraction Worker, e.g. "https://nest-listing-extractor.<account>.workers.dev"
  // or "http://localhost:8787" for `npm run dev` in worker/. Leave blank to use sample extraction.
  extractionApiUrl: "",
};
