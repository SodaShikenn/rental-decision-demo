// Local-only configuration. Keep keys out of this file before committing.
window.RENTAL_DEMO_CONFIG = {
  googleMapsApiKey: "",
  // Base URL of the listing-extraction Worker (worker/). Blank keeps the sample extraction flow.
  extractionApiUrl: "",
};
